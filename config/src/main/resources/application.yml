server:
  port: 8091
spring:
  application:
    name: ConfigServer

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8761
#  spring:
#    cloud:
#      config:
#        i
#spring:
#  config:
#    import: optional:configserver:http//localhost:8099