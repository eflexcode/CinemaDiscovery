server:
  port: 8095
  servlet:
    context-path: /account/
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8761
spring:
  application:
    name: ACCOUNTSERVER

  data:
    mongodb:
      host: localhost
      port: 27017
      database: CinemaDiscoveryAccount

