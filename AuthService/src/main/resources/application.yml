server:
  port: 8095
  servlet:
    context-path: /auth/
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8761
spring:
  application:
    name: AUTHSERVER

  data:
    mongodb:
      host: localhost
      port: 27017
      database: CinemaDiscoveryUser
